<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>Daily Open Tickets Reminder</h1>
    <p>Hello <%= @agent.email %>,</p>
    <p>Here is the list of open tickets as of <%= Time.current.strftime('%Y-%m-%d') %>:</p>
    <% if @open_tickets.any? %>
      <table border="1" cellpadding="5" cellspacing="0">
        <thead>
          <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Description</th>
            <th>Created At</th>
            <th>Created By</th>
          </tr>
        </thead>
        <tbody>
          <% @open_tickets.each do |ticket| %>
            <tr>
              <td><%= ticket.id %></td>
              <td><%= ticket.title %></td>
              <td><%= ticket.description %></td>
              <td><%= ticket.created_at.strftime('%Y-%m-%d %H:%M:%S') %></td>
              <td><%= ticket.user.email %></td>
            </tr>
          </tbody>
        <% end %>
      </table>
    <% else %>
      <p>No open tickets at this time.</p>
    <% end %>
    <p>Best regards,<br>Your Support Team</p>
  </body>
</html>